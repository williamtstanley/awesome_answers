<!-- <div id="answer-<%#= answer.id %>"> -->
<div id="<%= dom_id(answer) %>">
  <%= answer.body %> (by: <%= answer.user.full_name %>)
  <% if can? :destroy, answer %>
    <%= link_to "Delete", question_answer_path(@question, answer), method: :delete, remote: true %>
  <% end %>
  <hr>
</div>

